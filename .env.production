# Configurações de Produção - Incode Academy Lead System
# =======================================================

# Servidor
PORT=3001
NODE_ENV=production

# Base de Dados
# SQLite (local) - usar caminho persistente no Railway
DATABASE_PATH=./backend/database/leads.db
# PostgreSQL (opcional) - descomente se usar PostgreSQL
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Segurança e CORS
CORS_ORIGIN=https://your-production-domain.com,https://your-netlify-domain.netlify.app
API_RATE_LIMIT=500

# Modo Evento (true = sem rate limiting para alta carga)
EVENT_MODE=false

# Integração n8n (configure com sua URL real)
N8N_WEBHOOK_URL=https://your-n8n-webhook-url-here

# Opcional: Migração automática SQLite → PostgreSQL
# MIGRATE_FROM_SQLITE=false